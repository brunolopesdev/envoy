import "./index.scss"

export const Header = () => {
    return (
        <header className="x_header">
            <nav className="x_navbar">
                <a href="#">List Movies</a>
                <a href="#">Add New Movie</a>
            </nav>  
        </header>
    )
}
